import type { NextApiRequest, NextApiResponse } from "next"
import type {
    Router as RPCRouterType,
    Rpc as RPCType,
    Resolver as RPCResolverType,
    ResolverNoStream as RPCResolverNoStreamType,
    
} from "@effect/rpc"

export type {
    NextApiRequest,
    NextApiResponse,
}
export type {
    RPCRouterType,
    RPCType,
    RPCResolverType,
    RPCResolverNoStreamType,
}
